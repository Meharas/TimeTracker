package timetracker.buttons;

import timetracker.icons.Icon;

/**
 * Stellt einen Button für ein Issue dar
 */
public class GlobalButton extends BaseButton
{
    public GlobalButton(final String text, final Icon icon)
    {
        super(text, icon);
    }
}